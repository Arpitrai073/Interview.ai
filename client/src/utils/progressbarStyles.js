import { buildStyles } from 'react-circular-progressbar';

export const circularProgressStyles = (textSize) => buildStyles({
    textSize,
    pathColor: "#10b981",
    textColor: "#ef4444",
    trailColor: "#e5e7eb",
})
